# v24 — AI 해설 실패 시 원인을 알 수 있도록 진단 정보 추가

## 문제

"최신 모형 자동 해설"이 `기본 요약(AI 미사용: Error: AI 응답에서 JSON 객체를 찾지
못했습니다)`로 폴백되는데, **AI가 실제로 뭐라고 답했는지는 전혀 안 보여서** 원인을 특정할
수 없었습니다.

## 원인 후보 (v24 배포 후 실제 응답을 보면 셋 중 하나로 좁혀짐)

1. Llama 모델이 "JSON만 출력하라"는 지시를 어기고 설명 문장을 앞뒤에 붙임 (가장 흔한 경우)
2. Workers AI 응답 구조가 예상(`res.response`)과 달라서 실제 텍스트를 못 읽어옴
3. `e.AI.run()` 호출 자체가 조용히 실패하거나 빈 응답을 반환

(모델 ID `@cf/meta/llama-3.3-70b-instruct-fp8-fast` 자체는 Cloudflare 공식 카탈로그에
현재 존재하는 유효한 모델로 확인했습니다 — 모델명 오타 문제는 아닙니다.)

## 수정

- `extractJsonObject()`가 실패하면 **AI 응답의 실제 앞부분(최대 180자)을 에러 메시지에
  그대로 포함**합니다.
- `e.AI.run()` 호출 자체가 던지는 에러도 별도로 잡아서 "Workers AI 호출 자체가
  실패했습니다: ..."로 구분해서 보여줍니다.
- AI가 빈 문자열을 반환한 경우 응답 구조 전체(JSON.stringify, 180자)를 에러에 포함합니다.
- `headline`/`summary` 필드가 없는 불완전한 JSON이 와도 원본 텍스트를 함께 보여줍니다.
- 프론트엔드: 에러 메시지가 길어졌으므로 배지 안에 욱여넣지 않고 별도 줄로 분리해 가독성을
  높였습니다.

## 다음 단계

배포 후 "최신 모형 자동 해설"을 다시 눌러보시면, 이번엔 노란 경고 텍스트에 **AI가 실제로
반환한 내용의 일부**가 보일 겁니다. 그 내용을 알려주시면:

- 설명 텍스트가 JSON 앞뒤에 섞여 있다면 → 프롬프트를 더 엄격하게 하거나, 정규식을
  더 관대하게(예: 첫 `{`부터 마지막 `}`까지가 아니라 균형 잡힌 중괄호 파싱으로) 바꿉니다.
- 응답 구조 자체가 다르게 생겼다면(`res.response`가 아닌 다른 필드) → 그 필드명에 맞춰
  파싱 로직을 수정합니다.
- Workers AI 호출 자체가 실패한다면(권한/바인딩 문제) → Cloudflare 대시보드에서 Workers AI
  사용 설정과 계정 한도를 확인해야 합니다.
