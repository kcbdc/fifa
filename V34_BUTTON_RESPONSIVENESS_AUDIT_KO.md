# v34 — 전체 버튼 반응성 전수 점검 (35개 버튼 감사)

## 점검 방법

`index.html`의 모든 `<button id="...">`를 추출해 `app.js`의 핸들러와 1:1 대조했습니다.
총 **35개 버튼** 전부 핸들러가 연결돼 있었지만(죽은 버튼 없음), 그중 **12개가 클릭해도
서버 응답이 올 때까지 화면에 아무 변화가 없어서** "클릭이 씹혔나?" 오해를 유발하는
상태였습니다.

## 발견된 무반응 버튼 12개

| 버튼 | 문제 |
|---|---|
| **모형 실행** (`runModel`) | 신고하신 바로 그 버그. POST 요청 + GitHub Actions 디스패치까지 1~2초 이상 걸리는데 그동안 "분석 상태" 패널이 그대로였음 |
| **상태 조회** (`checkModel`) | 동일 |
| **실행 전 검증** (`validateModel`) | 동일 |
| **품질검사** (`qualityBtn`) | 신고하신 바로 그 버그. 클릭해도 아무 표시 없음 |
| **ERGM 모형 비교** (`comparisonBtn`) | 동일 |
| **중앙성·커뮤니티 계산** (`analyticsBtn`) | 동일 |
| **프로젝트 생성** (`createProjectBtn`) | 동일 |
| **연도별 지표 계산** (`temporalBtn`) | 동일 |
| **데이터셋 동결** (`freezeBtn`) | 동일 |
| **재현성 보고서** (`reportBtn`) | 동일 |
| **GraphML·GEXF·BibTeX·RIS·APA 내보내기 5개** | 동일 (버튼 자체는 비활성화도 안 되고 텍스트도 안 바뀜) |

## 수정: 공통 래퍼 `withBusy()` 적용

버튼마다 따로 손으로 고치면 다시 빠뜨리기 쉬워서, **모든 버튼이 거치는 공통 함수**를
만들어 일괄 적용했습니다.

```js
function withBusy(btn, run, opts) {
  // 1) 클릭 즉시 버튼 비활성화 + 로딩 문구로 변경
  // 2) 이미 처리 중이면 재클릭 무시 (중복 요청 방지)
  // 3) 결과 영역이 있으면 그 영역에도 즉시 "처리 중" 문구 표시
  // 4) 에러는 반드시 화면에 표시 (조용히 사라지지 않음)
  // 5) 성공/실패 무관하게 버튼은 항상 원상복구
}
```

### 특히 신고해주신 두 가지

**"모형 실행" 버튼**: 클릭 즉시 버튼이 "요청 중..."으로 바뀌고 비활성화되며, 오른쪽
"분석 상태" 패널에도 바로 다음 문구가 뜹니다:
> 모형 실행을 요청하는 중입니다... (GitHub Actions 디스패치 포함, 몇 초 걸릴 수 있습니다)

**"품질검사" 버튼**: 클릭 즉시 버튼이 "검사 중..."으로 바뀌고, 품질 패널에도 "품질 검사를
실행하는 중입니다..."가 즉시 표시됩니다.

## 부가 효과: 중복 요청 방지 (계산 오류 원천 차단)

`withBusy()`는 버튼이 비활성화된 동안 재클릭을 무시합니다. 예전 대화에서 실제로 겪으신
**"모형 실행을 여러 번 눌러서 GitHub Actions가 5개나 중복 실행된"** 문제(#38~#42)가
이제 구조적으로 재발할 수 없습니다 — 첫 클릭 후 버튼이 즉시 잠기기 때문입니다.
`collectBtn`(배치 수집), `importBtn`(업로드) 등 부작용이 있는 다른 버튼들도 동일하게
보호됩니다.

## PNG/SVG 저장 버튼: 완료 확인 표시

즉시 완료되는 4개 버튼(PNG/SVG 저장)에는 로딩 상태 대신 **"저장됨 ✓"** 완료 확인을
1.2초간 표시하도록 `flashButton()`을 추가했습니다 — 파일이 실제로 받아졌다는 확신을
줍니다.

## 백엔드 재점검

"계산의 오류 발생 원천 차단" 요청에 따라 `src/index.ts` 전체에서 `JSON.parse()` 미보호
호출과 `.toFixed()` 미보호 호출을 전수 검색했습니다. 유일한 미보호 `JSON.parse`
(`fetchJSON`)는 이미 전역 에러 핸들러로 감싸져 있어 파싱 실패 시에도 500 에러 응답으로
안전하게 처리됩니다. `.toFixed()` 호출 8곳은 전부 이미 0으로 안전하게 기본값 처리된
계산식(`n>1?...:0` 패턴)의 결과라 NaN이 나올 수 없음을 확인했습니다.

## 검증

```
npm run check
# tests 15, pass 15, fail 0
```
35개 버튼 ID를 다시 전수 대조해 전부 정상적으로 연결돼 있음을 재확인했습니다.
마이그레이션 변경 없음 — `npm run deploy`만 하면 됩니다.
