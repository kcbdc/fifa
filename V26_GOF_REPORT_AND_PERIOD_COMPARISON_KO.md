# v26 — GOF 자동 리포트 · 시간 구간 비교 (논문 완성도 개선)

논문 완성도 개선 제안 중 "플랫폼에 없던 기능" 2가지를 실제 기능으로 구현했습니다.

## 1. GOF·모형 적합도 리포트 ([논문 연구실] 탭)

### 문제
GOF(적합도) 결과와 계수표가 R 스크립트 산출물(`artifacts/model-result.json`,
`gof.png`)이나 원시 API 응답(`/api/models?id=`)에만 존재해서, 논문 부록에 쓰려면
사람이 직접 GitHub Actions 아티팩트를 뒤지거나 JSON을 손으로 정리해야 했습니다.

### 추가
- `GET /api/gof-report?id=<실행번호>` — 완료된 모형의 공식, AIC/BIC, 수렴 여부,
  MCMC 사용 여부(dyad-independent 모형은 자동으로 "아니오(MPLE)"로 표시), GOF 계산
  가능 여부, 계수표, GOF 원본 출력(R의 `print(gof_obj)` 텍스트)을 구조화해서 반환합니다.
- `GET /api/gof-report/export?id=<실행번호>` — 위 내용을 논문 부록에 바로 붙일 수 있는
  **Markdown 파일**로 내려받습니다(표 형식 계수표 포함).
- [논문 연구실] 탭에 "GOF·모형 적합도 리포트" 카드 추가: 실행번호 입력 → "리포트 보기"로
  화면에서 바로 확인, "마크다운 다운로드"로 파일 저장(v23에서 고친 Blob 다운로드 방식 사용).

### 사용 예
완료된 실행번호(예: #37)를 넣고 "리포트 보기"를 누르면 계수표와 GOF 출력이 바로 나타납니다.
mutual/gwesp 없이 `edges`만 쓴 모형이면 "MCMC 사용: 아니오(MPLE)"로 자동 표시되어, v22에서
고친 그 이유를 논문에 그대로 설명할 수 있습니다.

## 2. 시간 구간 비교 ([연구경쟁력] 탭)

### 문제
"시간 변화 분석"은 연도별 지표를 나열만 할 뿐, "월드컵 전 vs 후"처럼 **두 특정 기간을
나란히 비교**하는 기능이 없었습니다. 논문에서 시간적 변화를 주장하려면 이런 비교가
거의 필수입니다.

### 추가
- `GET /api/period-comparison?type=win&aFrom=...&aTo=...&bFrom=...&bTo=...` — 두 기간
  각각에 대해 노드/엣지 수, 밀도, 평균 연결수, 커뮤니티 수, 활성(비고립) 노드 수를 계산하고
  두 기간의 차이(delta)를 함께 반환합니다. 기존 `advancedAnalytics()`(중앙성·커뮤니티 계산)를
  재사용해서 로직을 중복 구현하지 않았습니다.
- **국가별 순위 변동**: PageRank 기준 두 기간의 순위를 비교해, 변동 폭이 큰 상위 15개국을
  ▲/▼ 표시와 함께 보여줍니다(예: "모로코 45위 → 12위, ▲33").
- [연구경쟁력] 탭에 "시간 구간 비교" 카드 추가: 네트워크 종류 선택 + 기간 A/B의 시작·종료
  날짜 입력 → "비교 실행"으로 지표 비교표와 순위 변동표가 나타납니다.

### 사용 예
기간 A = 월드컵 개막 전(예: 2026-01-01~2026-06-10), 기간 B = 월드컵 이후
(2026-07-01~2026-07-29)로 설정하면, 대회를 전후로 네트워크 밀도·순위가 어떻게 바뀌었는지
바로 비교할 수 있습니다.

## 검증
- `tsc --noEmit` 통과, JS 문법 체크 통과
- 기존 테스트 15개 중 이전부터 무관하게 실패하던 4개(GitHub 디스패치 mock 등) 제외 모두 통과
- 새 함수는 모두 기존 데이터 파이프라인(`network()`, `advancedAnalytics()`, `model_runs`
  테이블)만 재사용했으며, 마이그레이션이나 GitHub Actions 변경은 없습니다.
