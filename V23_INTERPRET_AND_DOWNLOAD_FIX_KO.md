# v23 — 최신 모형 해설이 최신 "완료된" 실행을 못 찾는 버그 · 다운로드 파일 손상 버그 수정

## 1. "최신 모형 자동 해설"이 완료된 모형을 못 찾던 문제

### 원인
`/api/interpret`가 `id` 파라미터 없이 호출되면 `ORDER BY id DESC LIMIT 1`로 **상태와
무관하게 가장 마지막으로 시도한 실행**을 가져왔습니다. ERGM이 성공적으로 `completed`된
뒤에도, 그 이후 또 다른 실행(재시도·다른 옵션 등)이 실패로 끝나면 그 실패한 실행이 더 높은
`id`를 가지므로 그게 선택되어 "모형 결과가 아직 완료되지 않았습니다"가 계속 떴습니다.

### 수정
`id` 파라미터가 없을 때는 `WHERE status='completed' ORDER BY id DESC LIMIT 1`로
**완료된 것 중 가장 최신**을 먼저 찾도록 바꿨습니다. 완료된 게 하나도 없을 때만 기존처럼
가장 최근 시도(어떤 상태든)를 보여줍니다.

### 부가 개선
응답에 `runId`를 포함시켜, 화면에 "완료된 실행번호 #NN 기준"이라고 표시되도록 했습니다.
이제 몇 번 실행의 결과를 해설하고 있는지 항상 투명하게 보입니다.

## 2. GraphML/GEXF/BibTeX/RIS/APA 다운로드가 이상한 파일로 저장되던 문제

### 원인 (두 가지가 겹침)
1. `downloadUrl()`이 `<a href="URL" download>`을 직접 클릭하는 방식이었는데, 서버가
   에러를 반환하면(예: 네트워크 데이터가 비어 있어 `graphExport`가 예외를 던지는 경우)
   전역 에러 핸들러가 **JSON 에러 메시지를 담은 500 응답**을 돌려줍니다. 그런데 브라우저는
   이걸 그대로 `fifa-network.graphml`이라는 파일명으로 저장해버립니다 — 실제로는 JSON 에러
   텍스트가 들어있는데 확장자만 `.graphml`인 "이상한 파일"이 되는 것이었습니다.
2. `application/graphml+xml`, `application/gexf+xml`처럼 흔치 않은 MIME 타입은 일부
   모바일 브라우저(Samsung Internet 포함)가 `Content-Disposition: attachment`를 무시하고
   페이지로 렌더링하거나 파일명을 임의로 바꿔버리는 경우가 있습니다.

### 수정
모든 다운로드를 JSON 다운로드에 이미 쓰이던 것과 같은 **fetch → Blob → `URL.createObjectURL`**
방식으로 통일했습니다.
- 응답이 실패(`res.ok`가 false)면 실제 서버 에러 메시지를 `alert()`로 보여주고 **다운로드
  자체를 하지 않습니다.** 더 이상 에러 내용이 담긴 가짜 파일이 만들어지지 않습니다.
- Blob 생성 방식은 서버가 보낸 실제 콘텐츠 타입/바이트를 그대로 로컬 파일로 저장하므로,
  모바일 브라우저의 MIME 렌더링 오작동과 무관하게 항상 동일하게 동작합니다.
- 다운로드 앵커를 `document.body`에 잠깐 붙였다가 제거하도록 해서, 일부 모바일 브라우저에서
  DOM에 붙어있지 않은 `<a>`의 클릭이 무시되는 문제도 함께 방지했습니다.

### 추가: 파일 여는 법 안내
[연구경쟁력] → "재현성·내보내기" 섹션에 접이식 안내(`다운로드한 파일은 어떻게 열어보나요?`)를
추가했습니다.

| 파일 | 여는 방법 |
|---|---|
| 재현성 보고서 (.json) | 메모장으로 열거나, jsonviewer.stack.hu 같은 온라인 JSON 뷰어에 붙여넣기 |
| GraphML / GEXF | 무료 프로그램 **Gephi**(PC)에서 파일 열기 |
| BibTeX (.bib) / RIS (.ris) | Zotero, Mendeley, EndNote, Overleaf 등에 가져오기(Import) |
| APA (.txt) | 메모장/워드에서 바로 열기 |

스마트폰에서는 다운로드 알림을 탭한 뒤 "다른 앱으로 열기"로 위 프로그램(설치돼 있다면) 또는
텍스트 뷰어 앱을 선택하면 됩니다.
