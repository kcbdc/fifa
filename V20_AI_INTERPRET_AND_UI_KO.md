# v20 — 관리자 토큰 UI 제거 · 업로드 진행률 · 실제 AI 모형 해설

## 1. 관리자 토큰 UI 제거

v19에서 추가했던 헤더의 "관리자 토큰" 입력란을 제거했습니다. 백엔드의 `auth()` 함수는
`!e.ADMIN_TOKEN || ...` 로 되어 있어, **`ADMIN_TOKEN`이 설정되지 않은 배포본에서는 애초에
인증이 필요 없습니다.** 이번 프로젝트는 그 값을 설정하지 않은 상태이므로, 매번 헤더에 값을
넣게 만드는 것보다 UI를 없애는 것이 맞습니다.

- `public/index.html`: 헤더의 `.admin-auth` 블록 삭제
- `public/app.js`: `api()`를 원래의 단순한 형태로 되돌림(토큰 자동 첨부 로직 제거)
- `public/style.css`: 관련 스타일 삭제

> 참고: 만약 나중에 `wrangler secret put ADMIN_TOKEN`으로 실제 토큰을 다시 설정하게 되면,
> 이번에 지운 것과 같은 방식의 인증 UI가 다시 필요합니다. 지금은 그 값이 없는 상태를
> 전제로 되돌렸습니다.

## 2. CSV/JSON 업로드 진행률 표시줄

`fetch()`는 업로드 바이트 진행률 이벤트를 제공하지 않기 때문에, `XMLHttpRequest` 기반의
`uploadWithProgress()` 헬퍼를 새로 추가했습니다.

- `xhr.upload.onprogress`로 실제 업로드된 바이트 비율을 계산해 진행률(%)을 표시합니다.
- 업로드가 100%에 도달한 뒤 서버 응답을 기다리는 구간은 "서버 처리 중..."으로 별도 표시해,
  업로드 완료와 D1 저장 완료를 혼동하지 않도록 했습니다.
- 완료 후에는 저장/건너뜀 건수를 표시하고, 4초 뒤 진행률 표시줄을 자동으로 숨깁니다.
- 실패 시에도 `#log`에 원인이 표시됩니다(v19에서 고친 에러 처리 유지).

파일 자체가 보통 수백 KB 수준이라 진행률이 매우 빠르게 지나갈 수 있지만, 큰 파일을 올릴
때는 단계별로 명확하게 보입니다.

## 3. "최신 모형 자동 해설" — 실제 Cloudflare Workers AI 연동

기존에는 `deterministicInterpretation()`이라는 **규칙 기반 템플릿 함수**가 AIC/BIC 유무만
보고 문장을 채워 넣는 방식이었고, 실제 AI 모델을 호출하지 않았습니다. 이번에 이걸 실제
[Cloudflare Workers AI](https://developers.cloudflare.com/workers-ai/) 호출로 교체했습니다.

- `wrangler.jsonc`에는 이미 `"ai": {"binding": "AI"}`가 설정돼 있었고 `Env.AI` 타입도
  선언돼 있었지만, 실제로 쓰이는 곳이 없었습니다. 이번에 처음 연결했습니다.
- 기본 모델: `@cf/meta/llama-3.3-70b-instruct-fp8-fast` (환경변수 `WORKERS_AI_MODEL`로
  다른 Workers AI 모델로 교체 가능)
- `buildInterpretPrompt()`가 완료된 모형의 `formula`, `AIC/BIC`, `converged`,
  `coefficients`(term/estimate/std_error/p_value/odds_ratio), 노드·엣지·데이터 규모를
  한국어 프롬프트로 구성하고, **순수 JSON만 출력하도록** 지시합니다.
- 응답에서 `{...}` 블록을 정규식으로 추출해 파싱합니다(마크다운 코드펜스나 앞뒤 설명이
  섞여 나와도 견딤 — 3가지 응답 형태로 직접 테스트해 확인).
- **실패 시 자동으로 기존 결정론적 요약으로 대체**됩니다(모형이 아직 `completed` 상태가
  아닌 경우, AI 호출 자체가 실패한 경우, 응답에서 JSON을 못 찾은 경우, 필수 필드가
  없는 경우). 화면에는 어느 쪽인지 배지로 항상 표시됩니다.
  - `● AI 생성(모델명)` — Workers AI가 실제로 생성
  - `● 기본 요약(AI 미사용: 에러 메시지)` — 폴백 사용, 원인도 함께 표시

### 프론트엔드

- "최신 모형 자동 해설" 버튼에 로딩 상태(`AI 분석 중...`, 안내 문구)를 추가해 몇 초간
  기다려야 한다는 걸 사용자가 알 수 있게 했습니다.
- 응답의 `coefficientNotes`(계수별 해석)를 새 섹션으로 렌더링합니다.

### 주의

- Workers AI 요청은 계정의 Workers AI 사용량/요금에 포함됩니다. 버튼을 누를 때마다
  호출되므로, 트래픽이 많다면 캐싱이나 호출 제한을 추가로 고려하십시오.
- AI가 생성한 해설은 여전히 초안 참고용입니다. 기존 "논문 활용 체크리스트"의
  "AI 해설은 연구자가 재검토" 항목은 이번 변경 이후에도 동일하게 유효합니다 —
  오히려 실제 LLM이 생성하는 만큼 그 원칙이 더 중요해졌습니다.
