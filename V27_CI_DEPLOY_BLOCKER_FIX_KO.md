# v27 — GitHub Actions 배포 실패 원인 수정 (긴급)

## 발견 경위

"Add files via upload #26" 커밋의 `Deploy Cloudflare Worker` 워크플로가 14초 만에
**Failure**로 끝났습니다. 로그를 보니 `deploy.yml`의 `npm run check` 단계에서 테스트
4개가 실패해 그 다음 단계인 `wrangler deploy` 자체가 실행되지 않고 있었습니다.

```yaml
- run: npm ci
- run: npm run check      # ← 여기서 실패하면 아래 배포 단계가 아예 스킵됨
- uses: cloudflare/wrangler-action@v3   # 실행 안 됨
```

## 원인

`tests.mjs`에 있는 4개 테스트(v10, v12, v13, v15)가 전부 같은 이유로 실패하고 있었습니다:

```js
assert.match(worker, /VERSION='16\.0\.0'/);
```

`src/index.ts`의 실제 `VERSION` 상수는 이미 오래전에 `'17.0.0'`으로 올라가 있었는데,
테스트 파일은 예전 `'16.0.0'` 문자열을 그대로 하드코딩해서 찾고 있었습니다. **이건 이번
대화에서 제가 만든 문제가 아니라, 애초에 업로드해주신 v17 zip에 이미 있던 결함**입니다 —
버전이 v10 → v16으로 올라가는 과정 어딘가에서 `VERSION` 상수만 갱신되고 테스트는 안
갱신된 것으로 보입니다.

지금까지 로컬(제 작업 환경)에서 `npm test`를 돌릴 때마다 "15개 중 4개는 원래부터 실패,
이번 변경과 무관"이라고 판단하고 넘어갔던 게 바로 이 4개인데, **로컬 판단으로는 맞는
말이었지만 실제 GitHub Actions 배포 파이프라인에서는 이게 배포 전체를 막는 치명적인
문제였습니다.**

## 수정

4곳의 하드코딩된 `VERSION='16.0.0'` 정규식을 버전 숫자에 상관없이 통과하도록
`VERSION='\d+\.\d+\.\d+'`로 바꿨습니다. 테스트의 원래 의도(VERSION 상수가 존재하고 올바른
형식인지 확인)는 그대로 유지하면서, 앞으로 버전이 또 올라가도 다시 깨지지 않게 했습니다.

## 확인

```
npm run check
# ...
# tests 15
# pass 15
# fail 0
```

15개 전부 통과 확인했습니다. 이제 `git push`로 배포하면 `npm run check` 단계를 통과해서
실제로 `wrangler deploy`까지 실행됩니다.

## 왜 지금까지는 몰랐나

이전에 드린 v18~v26 zip들을 **GitHub Actions(push 트리거)로 배포하려 하셨다면, 그때마다
전부 이 4개 테스트 때문에 배포가 막혔을 가능성이 있습니다.** 만약 로컬에서
`npm run deploy`(테스트를 거치지 않는 명령)로 직접 배포해오셨다면 문제없이 반영됐을
것이고, 실제로 v23 이후 화면에 반영된 변경사항들(완료된 실행번호 표시, AI 에러 메시지
개선 등)이 확인됐던 걸 보면 그렇게 배포해오신 것으로 보입니다. 이번처럼 GitHub 웹의
"Add files via upload"로 직접 push하신 경우에만 이 문제가 드러난 것 같습니다.
