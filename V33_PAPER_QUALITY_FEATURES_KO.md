# v33 — 논문 퀄리티 개선 제안 1~5번 전체 구현

## 1. 경기 유형 필터 (공식 경기 vs 친선전)

**중요한 발견**: `matches` 테이블의 `stage`/`importance` 컬럼은 스키마에는 있지만
수집 코드(`taskMatches()`)가 실제로 채운 적이 없어서(`stage`는 항상 NULL,
`importance`는 항상 기본값 10) 필터 기준으로 쓸 수 없었습니다. 대신 실제로 채워지는
`competition` 필드(원본 소스의 `tournament` 컬럼)를 사용했습니다 — 친선전은 정확히
`"Friendly"` 값을 가집니다.

- `network()`에 `matchType` 파라미터 추가: `all`(기본, 기존과 동일) / `official`(친선전
  제외) / `friendly`(친선전만)
- [네트워크 탐색기]와 [고급분석] 탭에 선택창 추가
- 값이 다르면 자동으로 별도 URL이 되어 v28의 엣지 캐싱과도 자연스럽게 맞물립니다

## 2. 무작위 네트워크(귀무모형) 대비 비교

완전 무작위 그래프(Erdős–Rényi, 같은 노드 수·같은 밀도)와 비교해 실제 네트워크가 얼마나
"비무작위적"인지 정량화합니다.

- 전역 군집계수(clustering coefficient, transitivity)를 실제 삼각형 계산으로 구함
- ER 무작위 그래프의 이론값: 군집계수 ≈ 밀도(p), 차수분산 ≈ n·p·(1-p)
- 관측/기대 비율로 "몇 배나 더/덜 군집돼 있는가"를 자동 해석 문장으로 제공
- [고급분석] 탭에 "무작위 네트워크(귀무모형) 대비 비교" 카드 추가
- 손으로 만든 삼각형 포함 소규모 그래프로 직접 검증(군집계수 0.5 정확히 산출 확인)

## 3. 대륙연맹별 네트워크 이질성

"같은 대륙연맹" 효과가 모든 대륙에서 동일하게 작동하는지 확인할 수 있는 구조적 지표를
제공합니다.

- 대륙연맹별 하위 네트워크(그 연맹 소속 국가 간 연결만)의 밀도·평균연결을 계산
- 연맹 내부 연결 비율(withinShare) — 대륙 간 vs 대륙 내 연결 비중
- [고급분석] 탭에 "대륙연맹별 네트워크 이질성" 카드 추가
- **한계**: 이건 R로 대륙별 ERGM을 실제 재적합하는 게 아니라, Worker에서 즉시 계산 가능한
  구조적 지표입니다. 대륙별 밀도가 크게 다르면 이를 근거로 [ERGM] 탭에서 데이터를
  대륙별로 나눠 별도 모형을 돌려보시라고 안내 문구를 넣었습니다.

## 4. 효과 크기의 실질적(확률 단위) 해석

log-odds만으로는 "그래서 실질적으로 얼마나 큰 효과인가"가 안 와닿는 문제를 해결합니다.

- `edges` 계수를 기준 확률(baseline)로 보고, 로지스틱 함수(sigmoid)로 각 계수의 1단위
  증가가 tie 형성 확률을 몇 %p 바꾸는지 환산
- 예: "edges만 있을 때 기준 확률 1.48%에서, 같은 대륙 여부가 있으면 약 3.56%로 +2.08%p
  변화"
- **비선형성 주의사항을 항상 함께 표시**: "다른 모든 항이 0(기준값)일 때"라는 전제가
  붙는 근사치라는 점을 리포트에 명시
- GOF 리포트 화면과 Markdown 다운로드 모두에 반영

## 5. 출판급 벡터(SVG) 이미지 내보내기

네트워크 그래프가 캔버스(래스터)로만 존재해서 논문에 확대 삽입하면 깨지는 문제를
해결했습니다.

- **PNG 저장**: 캔버스를 그대로 `canvas.toDataURL()`로 다운로드(빠른 공유용)
- **SVG 저장(논문용)**: `draw()`와 동일한 원형(대륙연맹별) 레이아웃 알고리즘을 SVG
  마크업으로 다시 구현 — 완전한 벡터라 논문에 확대 삽입해도 깨지지 않고, Illustrator/
  Inkscape에서 라벨·색상을 편집할 수 있습니다
- 대시보드와 네트워크 탐색기 양쪽 모두에 버튼 추가

## 검증

```
npm run check
# tests 15, pass 15, fail 0
```
`tsc --noEmit` 통과. 효과 크기(sigmoid) 계산과 군집계수(triangle-counting) 로직은
Node.js로 독립 실행해 수치 정확성을 직접 검증했습니다. 마이그레이션 변경 없음 —
`npm run deploy`만 하면 됩니다.
