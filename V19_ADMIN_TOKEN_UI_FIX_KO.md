# v19 — 관리자 인증 프론트엔드 버그 수정

## 발견된 문제

`ADMIN_TOKEN`이 설정된 배포본에서, 관리자 권한이 필요한 화면 기능들이 조용히 실패하고 있었습니다.

1. **`public/app.js`의 `api()` 헬퍼가 `Authorization` 헤더를 아예 보내지 않음.**
   백엔드의 `auth(r,e)`는 `ADMIN_TOKEN`이 설정된 경우 `Authorization: Bearer <token>`이 없으면
   401을 반환합니다. 그런데 프론트엔드 어디에도 이 헤더를 붙이는 코드가 없었습니다
   (`grep -c authorization public/app.js` → 0건). 즉 `ADMIN_TOKEN`을 설정하는 순간, 브라우저
   화면에서는 "다음 배치 수집", "처음부터 재설정", "CSV/JSON 업로드(가져오기)", 프로젝트
   생성, 데이터셋 동결 등 관리자 전용 버튼이 전부 401로 실패하게 됩니다.

2. **`#importBtn`(가져오기) 핸들러에 `try/catch`가 없었음.**
   1번 문제로 401이 나면 `api()`가 예외를 던지는데, 이 핸들러만 유일하게 `try/catch`가 없어서
   예외가 처리되지 않은 프로미스 거부(unhandled rejection)로 사라집니다. `#log` 영역이 갱신되지
   않으니 사용자 입장에서는 버튼을 눌러도 "아무 반응이 없는" 것처럼 보였습니다. (다른 버튼들은
   `try/catch`가 있어 최소한 에러 메시지라도 `#log`에 표시됩니다.)

3. **`#normalizeBtn`("FIFA 211 정규화") 버튼에 클릭 핸들러 자체가 없었음.**
   `index.html`에는 버튼이 있지만 `app.js` 어디에도 `$('#normalizeBtn').onclick`이 정의돼 있지
   않아, 이 버튼은 애초에 항상 아무 동작도 하지 않는 죽은 버튼이었습니다.

## 수정 내용

- **관리자 토큰 입력 UI 추가**: 헤더 우측에 `관리자 토큰` 입력란과 `저장` 버튼을 추가했습니다.
  값은 `localStorage`(`fnl_admin_token`)에 저장되고, 저장 여부가 옆에 상태로 표시됩니다.
- **`api()` 헬퍼가 저장된 토큰을 모든 요청에 자동으로 첨부**하도록 변경했습니다(호출부마다
  일일이 헤더를 추가할 필요 없음). 401이 나면 "인증 실패: 우측 상단에서 관리자 토큰을
  저장하세요"라는 안내 메시지를 던지도록 했습니다.
- **`#importBtn`에 `try/catch` + 로딩 상태 + 따옴표를 포함한 CSV까지 처리하는 견고한 파서**를
  추가했습니다. 실패 시 반드시 `#log`에 원인이 표시됩니다.
- **`#normalizeBtn`에 실제 핸들러를 추가**하여 `POST /api/fifa-normalize`를 호출하도록
  했습니다.

## 확인 방법

1. 배포 후 화면 우측 상단 "관리자 토큰" 입력란에 `ADMIN_TOKEN`과 동일한 값을 넣고 저장.
2. [데이터] 메뉴에서 CSV를 다시 업로드해보면 이번에는 성공/실패 여부가 `#log`에 명확히 표시됩니다.
3. "FIFA 211 정규화" 버튼도 이제 정상적으로 동작합니다.

## 참고: 토큰 저장 방식에 대한 주의

이번 수정은 `localStorage`에 토큰을 평문으로 저장하는 가장 단순한 방식입니다. 여러 사람이
같은 브라우저/기기를 공유한다면 토큰이 노출될 수 있으니, 다중 사용자 운영 환경에서는 별도의
로그인 체계(세션 기반 인증 등)로 교체하는 것을 권장합니다. 지금 규모(개인 연구용 관리자 도구)에는
충분합니다.
